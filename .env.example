# Copy to .env and paste a GitHub personal access token (classic or fine-grained).
# Needs no scopes at all for public repos — it only lifts the rate limit
# from 60 requests/hour to 5000.
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=
